# JSON Formatting

Convert a Python object to a JSON string with formatting.

```python
json.dumps(data, indent=4, sort_keys=True, ensure_ascii=False)
```

indent: number of indentation spaces
sort_keys: sort dictionary keys alphabetically
ensure_ascii: display Cyrillic characters and other special characters correctly
