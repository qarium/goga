# Это тестовый топик который создан через `goga topics create` 





Сейчас я создаю описание задачи через терминал гога команд
Error: no base for the published branch — set topics.base_ref in .goga/config.yml or pass --base-ref:
topics:
  base_ref: origin/main
