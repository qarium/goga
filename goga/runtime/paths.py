"""Path composition routines for the goga runtime cell.

The routines declared in `goga/runtime/CODEMANIFEST` live here:
`normalize_project_path`, `resolve_git_branch`, `resolve_runtime_dir`.

They are implemented in a subsequent task; this file is the module marker that
exposes the import surface so the `goga.runtime` facade can re-export them.
"""
