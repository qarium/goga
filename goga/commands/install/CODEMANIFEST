Imports:
  - Types:
      - install AS install_logic
    From: goga/install
  - Types:
      - Config
      - load_config
    Usages:
      - configuration
    From: goga/config

Usages:
  click: .goga/usages/cooks/click.md
  conventions: https://raw.githubusercontent.com/qarium/goga-lang-conventions/refs/heads/0.0.x/python/project.md

Annotations: |
  Практика `conventions` используется для:
  - работы с кодовой базой
  - организации REPL цикла разработки
  - отладки и тестирования
  - организации тестовой инфраструктуры
  - понимания общих принципов/правил разработки и тестирования в проекте
  Для создания команды использовать практику `click`.
  Команда делегирует бизнес-логику через `install_logic`.

---

"install(agent: str | None = None, force_overwrite: bool = False)":
  location: install.py
  annotations: |
    CLI-обёртка команды установки. Делегирует бизнес-логику `install_logic`.

    `agent`: целевой AI-агент
    `force_overwrite`: разрешить перезапись существующих скиллов

    CLI-опции:
    - --agent: целевой AI-агент
    - --force-overwrite: перезаписывать существующие скиллы при установке из инструментов

    Алгоритм:
    1. Загрузить конфигурацию через `load_config` (практика `configuration`) → получить `Config`
    2. Вызвать `install_logic`(`agent`, `Config`, `force_overwrite`)
    3. Вернуть exit_code

---

Author: Goga
CreatedAt: 20/05/26

Description: |
  CLI-обёртка команды установки скиллов goga