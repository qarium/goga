# Contract — Hooks

The contract domain exposes **no hook actions** for tool packages today.

The comparison is a read-only analysis over the project files and the parsed manifests. The platform mechanism behind every hook action is covered in [Hooks](../hooks/index.md).
