# Connect — Configuration

The connect domain reads **no section of the project `.goga/config.yml`**.

Its state lives at the home level instead: the connected agents are recorded in `~/.goga/connect.yml` with each agent's persisted `force_overwrite` flag — see [Configuration — Home](../../configuration/home.md). The general configuration model of the product is covered in [Configuration](../../configuration/index.md).
