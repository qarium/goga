# Usages — Hooks

The usages domain exposes **no hook actions** for tool packages today.

The sync is driven purely by the configuration declarations (see [Configuration](configuration.md)). The platform mechanism behind every hook action is covered in [Hooks](../hooks/index.md).
