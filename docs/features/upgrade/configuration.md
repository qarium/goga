# Upgrade — Configuration

The upgrade domain reads **no section of `.goga/config.yml`** — the version line is derived from the installed package, and the agents to re-sync come from the home state (`~/.goga/connect.yml`, see [Configuration — Home](../../configuration/home.md)).

The general configuration model of the product is covered in [Configuration](../../configuration/index.md).
