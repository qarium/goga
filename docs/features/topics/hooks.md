# Topics — Hooks

The topics domain exposes **no hook actions** for tool packages today.

Topic identity, addressing, and statuses belong to the [History](../history/index.md) domain — the one hook action of that surface (the status-scale registration) is declared there: see [History — Hooks](../history/hooks.md). The platform mechanism behind every action is covered in [Hooks](../hooks/index.md).
