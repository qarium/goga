# Init — Hooks

The init domain exposes **no hook actions** for tool packages today.

Initialization is a one-time interactive flow over the goga assets; the tool packages enter the project afterwards — through [Install](../install/index.md) and [Connect](../connect/index.md). The platform mechanism behind every hook action is covered in [Hooks](../hooks/index.md).
