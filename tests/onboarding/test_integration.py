"""End-to-end onboarding session tests — placeholder for the invited-tool suite.

The old integration suite of this file drove the deleted flat-answer model
and the old per-field questionnaire; its cases were ported into the
leaf-cell test layout — ``questions/``, ``survey/``, ``generator/``, and
``test_logic.py`` — during the facade rewrite. The cross-entity suite of
the whole feature (invitation → dedup → both tool moments → survey →
amendments → generation → attributed report through the CLI) is rebuilt in
this file together with the end-to-end task of the plan.
"""
